{
  "extends": "../tsconfig.json",
  "include": ["core", "services", "extract", "test", "scripts"],
  "compilerOptions": {
    "types": ["services/config"],
    "module": "esnext",
    "moduleResolution": "node",
    "baseUrl": ".",
    "paths": {
      "@acme/core": ["./core"],
      "@serverless-stack/backend": ["./extract"]
    }
  }
}
